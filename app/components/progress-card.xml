<StackLayout class="card">
    <Label text="Seu Progresso" class="font-bold mb-2" />
    <Progress value="{{ progressValue }}" maxValue="100" class="mb-2" />
    <Label text="{{ progressText }}" textWrap="true" class="text-gray-600" />
</StackLayout>